# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Google OAuth Configuration
# -----------------------------------------------------------------------------
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your_secret_here

# -----------------------------------------------------------------------------
# JWT Secret for Session Management
# -----------------------------------------------------------------------------
# Generate a random 32+ character string for security
# Example: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_min_32_chars_long

# -----------------------------------------------------------------------------
# App URLs (Environment-Specific)
# -----------------------------------------------------------------------------
# Local Development:
NEXT_PUBLIC_APP_URL=http://localhost:5000
NEXT_PUBLIC_API_URL=http://localhost:5000

# Pre-Production (Vercel - main branch):
# NEXT_PUBLIC_APP_URL=https://humbug-quiz.vercel.app
# NEXT_PUBLIC_API_URL=https://humbug-quiz.vercel.app

# Production (Vercel - master branch):
# NEXT_PUBLIC_APP_URL=https://humbug.hu
# NEXT_PUBLIC_API_URL=https://humbug.hu

# -----------------------------------------------------------------------------
# Database Configuration (Neon PostgreSQL)
# -----------------------------------------------------------------------------
# Get these from: https://console.neon.tech
# Important: Use the POOLED connection string for serverless functions

# Main connection URL (pooled)
POSTGRES_POSTGRES_URL=postgresql://user:password@host.neon.tech/database?sslmode=require

# Alternative format connection strings (auto-generated by Vercel/Neon)
POSTGRES_DATABASE_URL=postgresql://user:password@host-pooler.neon.tech/database?sslmode=require
POSTGRES_DATABASE_URL_UNPOOLED=postgresql://user:password@host.neon.tech/database?sslmode=require

# Individual connection parameters (optional, for tools)
POSTGRES_POSTGRES_HOST=ep-xxx-pooler.c-2.eu-central-1.aws.neon.tech
POSTGRES_POSTGRES_USER=neondb_owner
POSTGRES_POSTGRES_PASSWORD=npg_xxx
POSTGRES_POSTGRES_DATABASE=neondb

# -----------------------------------------------------------------------------
# Vercel Configuration (Auto-populated by Vercel)
# -----------------------------------------------------------------------------
# These are automatically set by Vercel, no need to configure manually
# VERCEL_OIDC_TOKEN=xxx
